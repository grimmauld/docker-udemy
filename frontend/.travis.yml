sudo: required

services:
  - docker

before_script:
  - cd frontend
  
before_install:
  - docker build -f Dockerfile.dev -t grimmauld/docker-udemy .

script:
  - docker run -e CI=true grimmauld/docker-udemy npm run test